# waste-treatment

## build the image with the name waste-manage

docker build -t waste-manage .  

## prepares a container called "waste-manage" based off the image we just created

run -it --rm --name waste-manage waste-manage